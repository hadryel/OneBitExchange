require 'rails_helper'
 
RSpec.describe "Exchange Currency Process", :type => :system, js: true do
  it "exchange value" do
    # Stopped working after updating to use Javascript events (keyup/change)
  end

  it "change currency to exchange" do
    # Stopped working after updating to use Javascript events (keyup/change)
  end

  it "invert exchange button" do
    # Stopped working after updating to use Javascript events (keyup/change)
  end
end
